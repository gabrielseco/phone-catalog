/* import React from 'react';
import { shallow } from 'enzyme';
import { PhotoListContainerTesting } from './PhotoListContainer';

describe('PhotoListContainer suite', () => {
  it('renders PhotoListContainer without any state injected', () => {
    const component = shallow(<PhotoListContainerTesting />);
    expect(component).toBeDefined();
  });
});
 */

describe('Todo PhoneListContainer', () => {
  it('should todo', () => {});
});
